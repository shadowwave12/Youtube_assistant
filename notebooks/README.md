# Notebooks

Use this directory for exploratory Jupyter notebooks used to understand the existing prototype, test AI libraries, and validate ideas before moving stable code into `backend/app/`.

Notebooks are experiments and learning artifacts, not the production runtime.
