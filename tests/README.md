# Tests

This directory contains automated tests for the project.

- `unit/`: Small tests for isolated backend or frontend behavior.
- `integration/`: Tests for interactions between application pieces, such as API routes and services.
- `fixtures/`: Reusable test inputs and sample responses.

Tests will be added as production behavior is implemented.
